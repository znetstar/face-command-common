/**
 * Options that will be passed to the EigenFaceRecognizer instance.
 */
export default class EigenFaceRecognizerOptions {
    constructor(public components: number, public threshold: number) {

    }
}